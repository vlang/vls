fn main() {
    mut an := [1]
    an << 'test'
    an2 := an << 2
    1 * 'hey'
    concat := 'hello' + 3
    'mul' * 'tiply'
    invalid_eq := 1 != 'test'
    invalid_or := 1 && 3
}

---

(error "cannot append `string` to `[]int`" [2,10]-[2,16])
(error "array append cannot be used in an expression" [3,14]-[3,16])
(error "mismatched types `int` and `string`" [4,4]-[4,13])
(error "mismatched types `string` and `int`" [5,14]-[5,25])
(error "undefined operation `string` * `string`" [6,4]-[6,19])
(error "mismatched types `int` and `string`" [7,18]-[7,29])
(error "mismatched types `int` and `int`" [8,18]-[8,24])